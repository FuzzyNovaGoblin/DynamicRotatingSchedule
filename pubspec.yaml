name: schedule
description: A rotating schedual app

publish_to: 'none' 

version: 1.2.0+2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.0.1
  shared_preferences: ^2.0.5


dev_dependencies:
  flutter_test:
    sdk: flutter

  json_serializable: ^4.1.1
  build_runner: ^2.0.2
  flutter_launcher_icons: ^0.9.0


flutter_icons:
  image_path: "icon/icon.png"
  android: true
  ios: true


flutter:

  uses-material-design: true
